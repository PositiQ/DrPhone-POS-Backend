info:
  name: Update Sale Status
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:3000/api/sales/SALE-000001/status
  body:
    type: json
    data: |-
      {
        "status": "cancelled"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
